<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="cn.creditloans.core.dao.af.RulesetConditionDao">

	<resultMap id="resultMap" type="cn.creditloans.core.entity.af.RulesetCondition">
        <id property="id" column="id" />
		<result property="name" column="name" />
		<result property="description" column="description" />
		<result property="rulesetId" column="ruleset_id" />
		<result property="lhs" column="lhs" />
		<result property="operator" column="operator" />
		<result property="rhs" column="rhs" />
		<result property="type" column="type" />
		<result property="sequence" column="sequence" />
		<result property="createUserId" column="create_user_id" />
		<result property="createTime" column="create_time" />
		<result property="upadteUserId" column="upadte_user_id" />
		<result property="updateTime" column="update_time" />
    </resultMap>
    
    <!-- 插入单个 -->
    <insert id="insert" parameterType="cn.creditloans.core.entity.af.RulesetCondition">
    	<selectKey order="AFTER" keyProperty="id" resultType="java.lang.Integer">
		    SELECT currval('af_ruleset_condition_id_seq')
		</selectKey>
 		INSERT INTO af_ruleset_condition (name,description,ruleset_id,lhs,
 			operator,rhs,type,sequence,create_user_id,
 			create_time,upadte_user_id,update_time)
		VALUES (#{name}, #{description}, #{rulesetId}, #{lhs},
			#{operator}, #{rhs}, #{type}, #{sequence}, #{createUserId},
			now(), #{upadteUserId}, now());
    </insert>
    
    <!-- 修改单个 -->
    <update id="update"  parameterType="cn.creditloans.core.entity.af.RulesetCondition">
    	update af_ruleset_condition set name = #{name}, description = #{description}, 
    	            lhs = #{lhs}, operator = #{operator}, rhs = #{rhs}, 
    	            type = #{type}, sequence = #{sequence},
    	            upadte_user_id = #{upadteUserId},
					update_time = now() where id = #{id}
    </update>
    
    <!-- 删除单个 -->
    <delete id="delete" parameterType="int">
    	delete from af_ruleset_condition where id = #{id} 
    </delete>
    
    <!-- 查询单个 -->
    <select id="select" resultMap="resultMap" parameterType="int">
    	select * from af_ruleset_condition where id = #{id} 
    </select>
    
    <!-- 查询所有 -->
    <select id="selectAllInfos" resultMap="resultMap" parameterType="int">
    	select * from af_ruleset_condition
    </select>
    
	<!-- 查询总量，这里的需要根据实际条件进行查询 -->
	<select id="selectPageCount" resultType="int" parameterType="cn.creditloans.core.entity.af.RulesetCondition">
        select count(1) FROM af_ruleset_condition where 1=1
        <if test="name != null and '' != name">  
			and name like '%${name}%'  
		</if>
		<if test="description != null and '' != description">
			and description like '%${description}%' 
		</if>
    </select>
    
    <!-- 根据实际情况需要修改语句 -->
    <select id="selectPageList" resultMap="resultMap">
         select * from af_ruleset_condition  where 1=1 and ruleset_id = #{entity.rulesetId} 
         <if test="entity.name != null and '' != entity.name">  
			and name like '%${entity.name}%'  
		</if>
		<if test="entity.description != null and '' != entity.description">
			and description like '%${entity.description}%' 
		</if>
         order by create_time desc  limit #{pm.pageSize} offset #{pm.startCount}
    </select>
 <!-- 检查名字是否唯一 -->
    <select id="selectNameIsExit" resultType="int" parameterType="string">
    	select count(1)  from af_ruleset_condition where name = #{name}
    </select>
 <!-- 检查次序是否唯一 -->
    <select id="selectSequenceIsExit" resultType="int" parameterType="string">
    	select count(1)  from af_ruleset_condition where sequence = #{sequence}
    </select>
    <!-- 不包含rule中的condition -->
    <select id="selectConditionNotRule" resultMap="resultMap" parameterType="java.util.List">
    	select *  from af_ruleset_condition where 1=1
    	<if test="conditionList != null and conditionList.size() > 0"> 
    	     and name not in
    	  	 <foreach collection="conditionList" item="item" index="index" open="(" separator="," close=")">  
    	         #{item}
    	  	  </foreach>
    	  </if>
    </select>
    <!-- 是否存在关联纪录 -->
    <select id="selectCountByRulesetElementId" resultType="int" parameterType="int">
    	select count(1) from af_ruleset_condition where ruleset_id = #{rulesetElementId} 
    </select>
</mapper>